apiVersion: v1
kind: Service
metadata:
  name: sample-service
  labels:
    app: sample
spec:
  selector:
    app: sample
  ports:
    - name: service-http80
      port: 80
      targetPort: http80
    - name: service-http8080
      port: 8080
      targetPort: http8080